#![feature(
    coroutines,
    coroutine_trait,
    trait_alias,
    never_type,
    exhaustive_patterns
)]

mod examples {
    mod chaining;
    mod counter;
    mod modal_dialog;
    mod vending_machine;
}
